import { EstaEnEtapaPipe } from './esta-en-etapa.pipe';

describe('EstaEnEtapaPipe', () => {
  it('create an instance', () => {
    const pipe = new EstaEnEtapaPipe();
    expect(pipe).toBeTruthy();
  });
});
