import { EnviosSinEntregaPipe } from './envios-sin-entrega.pipe';

describe('EnviosSinEntregaPipe', () => {
  it('create an instance', () => {
    const pipe = new EnviosSinEntregaPipe();
    expect(pipe).toBeTruthy();
  });
});
